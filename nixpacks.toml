[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
# Install devDependencies too — typecheck/build need typescript, vite, vitest peers, etc.
cmds = ["pnpm install --frozen-lockfile --prod=false"]
